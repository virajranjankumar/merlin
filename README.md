Follow the steps below to run the server with the compiled website.

```bash
git clone https://github.com/virajranjankumar/merlin
cd merlin
gem install bunlder
bundle install
bundle exec jekyll serve
```